# スラッシュメニューで挿入

行頭で `/` を入力すると、見出し・リスト・チェックボックス・テーブル・コードブロックなどをすばやく挿入できます（Raw / Preview 共通）。

例:
- `/h1` `/h2` `/h3` … 見出し
- `/todo` … チェックボックス
- `/numbered` … 番号付きリスト
- `/table` … テーブル
- `/code` … コードブロック

Preview 上部のツールバーからも同じ操作ができます。
